export default theme => ({
  root: {
    minWidth: 0,
    flexGrow: 1
  },
  eventListDate: {
    marginBottom: theme.spacing.unit * 2,
    paddingLeft: theme.spacing.unit * 2
  },
  eventListGroup: {
    marginBottom: theme.spacing.unit * 2
  }
})
